/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['assets.aceternity.com','images.unsplash.com','aceternity.com','i.ibb.co'], // Add the domain here for external image loading
    },
  };
  
  export default nextConfig;